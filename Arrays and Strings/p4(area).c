#include<iostream>
using namespace std;
int calculate(float a)
{
float c;
c=a*a;
cout<<c;
}
int calculate(int r)
{
float area;
area=(float)(3.14*r*r);
cout<<area;
}
int main()
{
flaat a;
int r;
cin>>a>>b;
calculate(a);
calculate(r);
return 0;
}
